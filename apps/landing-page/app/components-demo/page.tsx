'use client';

// Demo temporarily disabled - requires @libs path resolution fix
export default function ComponentsDemo() {
  return (
    <div style={{ padding: '32px' }}>
      <p>Demo disabled. Enable @libs path alias to use.</p>
    </div>
  );
}